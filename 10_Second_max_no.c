#include<stdio.h>
#include<limits.h>
int main()
{
    int max,a[100],i,n;
    printf("how many number you wants to enter");
    scanf("%d",&n);
    for(i=0;i<n;i++)
    scanf("%d",&a[i]);
  max =a[0];
  second=INT
  for(i=1;i<n-1;i++)
  {
    if(max<a[i])
    max=a[i];
  
  }
  printf("max is %d",max);
  return 0;
}